#ifndef DIFFERRENTIAL_H
#define DIFFERENTIAL_H

#include "../tree/tree.h"

Node* Diff(struct Node* node, struct Tree* tree);
Node* TreeSimpler(struct Node* node, struct Tree* tree);

#endif //DIFFERENTIAL_H